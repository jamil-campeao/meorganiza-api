{
  "name": "meorganiza-api",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "prisma-migration": "npx prisma migrate dev --name init",
    "start": "npm run prisma-migration && npm run db:seed && node index.js",
    "dev": "npm run prisma-migration && npm run db:seed && npx nodemon index.js",
    "db:seed": "npx prisma db seed",
    "db:seed-test": "node prisma/seed-test-environment.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@prisma/client": "^6.14.0",
    "bcryptjs": "^3.0.2",
    "cors": "^2.8.5",
    "express": "^5.1.0",
    "jsonwebtoken": "^9.0.2",
    "node-fetch": "^3.3.2",
    "nodemailer": "^7.0.7"
  },
  "type": "module",
  "devDependencies": {
    "@types/nodemailer": "^7.0.2",
    "nodemon": "^3.1.10",
    "prisma": "^6.14.0"
  },
  "prisma": {
    "seed": "node prisma/seed.js"
  }
}
